{
  "name": "trader-backend",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "db:migrate": "bun run src/data/migrate.ts",
    "ingest": "bun run src/data/ingest.ts",
    "record-ticks": "bun run src/data/record-ticks.ts",
    "tick-stats": "bun run src/data/tick-stats.ts",
    "search-markets": "bun run src/data/search-markets.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
